# Install on the AWS box that runs the Gradio UI.
# No torch / cellpose / opencv needed here - the heavy work runs on RunPod.
gradio>=4.0.0
requests>=2.31.0
Pillow>=9.5.0
# Rescales oversized TIFFs and builds the upload preview / diameter guide.
numpy>=1.24
